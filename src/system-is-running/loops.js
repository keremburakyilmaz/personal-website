export const loops = [
  "scanning → drifting → returning → scanning",
  "effort → interruption → re-centering → interruption",
  "focus attempt → loss → retry → loss",
  "think → doubt → re-think → doubt",
  "rehearse → replay → rehearse",
  "breathe in → forget → breathe out → forget",
  "try → overthink → try again",
  "remember → flinch → remember",
  "plan → freeze → replan → freeze",
  "reach out → retract → rephrase → retract"
];
